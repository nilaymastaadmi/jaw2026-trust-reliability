# The dataset is a separate public repo, cloned in place. Not ours to commit.
#   git clone https://github.com/satvikGIKA/BITS-Hackathon-Dataset.git dataset
dataset/

# Generated artefacts — all reproducible from src/ + dataset/
work/

__pycache__/
*.pyc
.venv/
venv/
.DS_Store

# Locally generated question dumps and drafting notes. Not pipeline inputs --
# and a repo full of question JSON invites exactly the "lookup table keyed to
# question text" suspicion the rules warn about.
heldout_questions*.json
nic_test_questions.json
*prompt*.md
*.pptx
